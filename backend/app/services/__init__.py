"""AuthLens service implementations.

Namespace package for the stage service subtrees (policy, retrieval,
evidence, readiness, clarifications, disclosure, packet, verification,
form_draft, cases). Created by the integration agent per
docs/PARALLEL_EXECUTION.md; kept import-only so each subtree stays
independently ownable.
"""
