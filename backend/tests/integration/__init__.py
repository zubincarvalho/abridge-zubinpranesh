"""Integration-lead end-to-end and safety regression tests.

Exercise the fully wired backend (real Agents A–F implementations behind the
frozen ports) through the FastAPI test client in deterministic mode.
"""
