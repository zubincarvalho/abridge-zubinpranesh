# Dependency requests — Agent G (API)

None. The API layer uses only existing dependencies: `fastapi`, `pydantic`,
`pydantic-settings`, `uvicorn` (runtime) and `pytest`, `httpx`, `pyyaml`
(dev, for the TestClient and the OpenAPI-compatibility test).
