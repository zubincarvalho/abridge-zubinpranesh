# Dependency requests — Agent F (Orchestration, cases, timeline)

None. The orchestrator uses only the standard library (`threading`,
`concurrent.futures`) plus the already-declared `pydantic` via
`app.contracts`.
