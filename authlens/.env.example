# AuthLens frontend configuration — copy to `.env` (gitignored).
# The frontend needs NO Anthropic key (that lives in the backend). It only
# needs to know where the backend API is. Default matches the backend's
# documented port.
VITE_API_BASE_URL=http://localhost:8000/api
